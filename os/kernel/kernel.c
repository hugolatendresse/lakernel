#include "types.h"
#include "syscalls.h"

void kernel_main() {
    /* Initialize hardware, drivers, etc. */

    /* Enter an infinite loop */
    while (1) {
        /* Handle system calls, interrupts, etc. */
    }
}